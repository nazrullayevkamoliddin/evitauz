import {createSlice} from '@reduxjs/toolkit'


const waterSlice = createSlice({
    name:'water',
    initialState: {
        
    },
     reducers:{

    }
})

// eslint-disable-next-line
export const {} = waterSlice.actions

export const WaterSlice = waterSlice.reducer